// MUZA — image credits
// No place photographs are bundled yet, so photo credits are empty.
// When images are added under images/place-<id>.jpg, return their credit HTML here,
// keyed by place id, e.g.  if (id === 1) return 'Photo: <a href="...">MUZA</a>';
function photoCreditHtml(id){ return ''; }
